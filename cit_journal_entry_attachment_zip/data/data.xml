<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="action_attachments_download_account_move" model="ir.actions.server">
            <field name="name">Download Attachment</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="state">code</field>
            <field name="code">if records: action = records.action_attachments_download()</field>
        </record>

        <record id="action_attachments_download_account_move_pdf" model="ir.actions.server">
            <field name="name">Download Attachment PDF</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="state">code</field>
            <field name="code">if records: action = records.action_pdf_attachments_download()</field>
        </record>

        <record id="action_attachments_download_account_move_line" model="ir.actions.server">
            <field name="name">Download Attachment</field>
            <field name="model_id" ref="account.model_account_move_line"/>
            <field name="binding_model_id" ref="account.model_account_move_line"/>
            <field name="state">code</field>
            <field name="code">if records: action = records.action_attachments_download()</field>
        </record>

        <record id="action_attachments_download_account_move_line_pdf" model="ir.actions.server">
            <field name="name">Download Attachment PDF</field>
            <field name="model_id" ref="account.model_account_move_line"/>
            <field name="binding_model_id" ref="account.model_account_move_line"/>
            <field name="state">code</field>
            <field name="code">if records: action = records.action_pdf_attachments_download()</field>
        </record>
    </data>
</odoo>
